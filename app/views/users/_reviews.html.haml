%h4 Recent reviews
- if reviews.empty?
  .alert.alert-info.alert-big.text-center You have not written any review yet
- else
  %ul.list-unstyled.recent-reviews
    - reviews.take(5).each do |review|
      %li.clearfix
        %div= review.content
        .review-data
          %div.pull-left.raty{ data: { score: review.rating, number: 5} }
          %div.pull-left= review.created_at
          %div.pull-left= link_to review.product.title, category_product_path(review.product.category, review.product)