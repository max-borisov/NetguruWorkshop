.col-md-4.text-center.product-item
  %h4= link_to product.title, category_product_path(product.category, product)
  %p= link_to image_tag(product.preview.url(:medium), alt: product.title, title: product.title), category_product_path(product.category, product)
  %p.product-item-price= number_to_currency(product.price)