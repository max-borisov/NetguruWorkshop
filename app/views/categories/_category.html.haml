.col-md-4.category-item.text-center
  %h3
    = link_to category.name, category_products_path(category)
  %div.sub-header= "(has #{ pluralize(category.products.count, 'product') })"
  - if user_signed_in? && current_user.admin?
    = link_to '', category, title: 'Delete', method: :delete, data: { confirm: 'Are you sure ?' }, class: 'glyphicon glyphicon-remove'